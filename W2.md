operator is =,+,-
operand is the numbers

types of opperation

assignment operator
  operand1 = opperand2

arithmetic operator
  modulo (%)
  n%2 = 0 (even)
  n%1 = 1 (odd)
  
  increment
  n = 44
  ++n = n and then 1 = 45
  n++ = 1 and then n = 44

relational operator
  == (equal)
  != (not equal)
  ?: (conditional assignment)

conditional expretions
  if
  else

Logical operators
  && (and)
  || (or)
  ! (not)

if (17<=age && age<=25) printf("elligible\n")
else printf("not elligible\n")

Bitwise operator
  &(and)
  |(or)
  ^(xor)(binaries)
  ~(complement)(binaries)
  >>(shift right)(binaries)
  <<(shift left)(binaries)

point operator


  

  
  
  
  
